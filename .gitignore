# Secrets — real values live in the Portainer stack, never in the repo
.env
.env.production
!finsight-frontend/.env.production

# Editor / OS
.idea/
.vscode/
.DS_Store

# Claude Code local overrides
.claude/settings.local.json
