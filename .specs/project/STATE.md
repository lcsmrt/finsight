# State

**Last Updated:** 2026-07-25
**Current Work:** **Recurrence Model v2 — ✅ T1–T10 EXECUTED 2026-07-24/25; T11 (full-stack E2E) DEFERRED — no DB reachable this session.** This is the last open M3 item — it will close milestone M3 and resolve blocker B-001 once T11 passes. All 9 backend + 1 frontend code tasks committed on `main` in both repos (see `.specs/features/recurrence-model-v2/tasks.md` Progress Log for full commit-hash table): T1 `78d9959` (retire `frequency` + V9 drop column), T3 `3bad56a` (`Clock` bean), T4 `6445f9e` (generator open-ended + `generateForwardWindow`), T5 `5729f84` (`createSeries` open-ended + `generatedThrough`), T7 `aa082da` (`OpenEndedSeriesTopUpService` + locked due-query), T8 `4a8c30d` (dashboard on-read top-up trigger, `getSummary` stays readOnly), T9 `8b96b74` (`editSeries` bound/reopen P3) — backend final count **62 unit + 72 integration, all green**; frontend T2 `0cceb64` (removed dead field) + T10 `3981af8` (open-ended form + "Ongoing" badge) — **42 tests green** (T10 also fixed two real bugs it introduced-then-caught: a `format(undefined)` crash risk and both end-date `DatePicker`s silently discarding their clear action, both fixed pre-commit). **Only T11 remains**: full-stack E2E against a throwaway `dev_finsight_verify` copy DB (create open-ended series via the real frontend, verify 12-month look-ahead, simulate time-passing top-up idempotency, bound-then-stop via editSeries, partition-invariant + top-line-totals unaffected by frequency removal). **Blocked this session on DB access** — no local Postgres running, no SSH tunnel active; user chose to skip rather than start Postgres/reconnect (see L-003/L-005 below for the usual recheck steps). **Say "resume work" to run T11** once DB access is available, then close out (flip B-001 fixed, ROADMAP M3 → complete, TESTING.md new IT classes: `OpenEndedSeriesIT`, `OpenEndedTopUpIT`). — Prior line below.
- Prior: **M3 Test Foundation — ✅ ALL 21 TASKS EXECUTED & E2E-VERIFIED 2026-07-17** (`.specs/features/test-foundation/`, spec+design+tasks all closed). Backend `0b1bac3`→`05360ff` (T1–T10, T12–T15, T17–T18, 15 commits) + frontend `5a62459`→`0b9af42` (T11, T16, T19, T20, 4 commits), both on `main` in both repos, working trees clean. Delivered: a Testcontainers-Postgres integration harness (`AbstractIntegrationTest`/`TestContainersConfig`/`TestAuthHelper`/`Fixtures`) backing 10 new `*IT` classes (split invariant, dashboard partition invariant, full plan-authz matrix, auth slice, transaction CRUD full-replace, series generate/edit/guards, migrations-apply-clean, CSV dedup, invitation lifecycle) plus a JaCoCo branch-coverage floor (0.80) on `SplitResolver`/`CategoryBreakdownAssembler`/`PlanAuthorization`; and on the frontend, a `unit` vitest project (jsdom) with real tests for `CreatePlanDialog`'s schema, `TransactionFormDrawer`'s schema/defaults/payload/edit-reset (both plain and series modes), `usePlanService`'s URL-scoping+invalidation, and a red/green-proven Base UI compound-component interaction guard on `Dropdown.stories.tsx` (closes the L-002 failure class, not just the one prior instance). Final counts: backend 55 unit + 58 integration tests, `./mvnw clean verify` green; frontend 37 unit tests + Storybook's 42 story tests, `npm run test` green. **Two real pre-existing bugs found (not fixed, logged below): B-002** (login with an unrecognized email returns 500 instead of 401) **and B-003** (negative top-level transaction amount is accepted, not rejected). `TESTING.md` and `CONCERNS.md` updated (T21) to reflect the new harness and downgrade the "no backend/frontend test coverage" concerns to "narrow but real, not zero." **This machine's Docker Desktop needs `DOCKER_HOST=unix:///home/lcs/.docker/desktop/docker.raw.sock` + `TESTCONTAINERS_RYUK_DISABLED=true` exported before `./mvnw verify` will work** — see Lesson L-008 for the full diagnosis (a real testcontainers/Docker-29 API-version bug, fixed by a committed `docker-java.properties`, plus this machine's non-standard socket topology, which is not committed/portable). ROADMAP.md M3 updated: schema migrations retroactively marked shipped (they landed during Shared Plans, 2026-07-11, not "planned" as the doc had stale-stated); "Recurrence Model v2" (the `frequency` enum, B-001) split out as its own not-yet-started forecast-track item. — Prior planning line below.
- Prior: **Series Edit (P1+P2) — ✅ ALL 13 TASKS EXECUTED & E2E-VERIFIED 2026-07-16. Requirements SEDIT-01..12 → Verified.** Backend `77b16d1`→`89bab38` (T1–T7 + a T13-discovered bugfix) + frontend `23be417`→`79b35de` (T8–T11), both on `main` in both repos, working trees clean. New `RecurrenceDefinition`(+participants child) entity owns each series' forward template (V8 migration, additive, metadata-only backfill); occurrences gain a nullable FK; one `PUT /series/{seriesId}` branches on scope (THIS_ONE/THIS_AND_FOLLOWING/ALL); k/N position-derived via pure `SeriesRegenerator` (8 unit tests); FE `SeriesScopeDialog`+`SeriesEditDrawer` wired as an "Edit series" row action. **Bug found+fixed during T13 E2E:** THIS_ONE's participant `clear()`+re-add (and the identical pattern in `SeriesRegenerator.stampOccurrence`) raced Hibernate's flush order → unique-constraint violation whenever a participant was retained across an edit; fixed by reconciling by member-id key instead (mirrors the pre-existing `applyParticipants` fix). **T12/T13 results:** all three scopes, P2 count increase/decrease, recurring end-date extend/shorten, `MAX_OCCURRENCES` overflow→400, D10 guard→400, fail-closed CONTRIBUTOR auth (no partial writes), dashboard totals cross-checked against raw SQL, and a real-Chromium FE click-through (row action → scope dialog → prefilled drawer → submit → toast → table refresh, zero Base UI runtime errors) all passed against a throwaway `dev_finsight_verify` copy — see Lesson L-007 below for a deviation encountered along the way (real DB auto-migrated by the user's own dev server before the copy-DB gate ran; verified safe, no corruption). Full detail in `.specs/features/series-edit/tasks.md` Progress Log. — Prior planning line below.
- Prior: **Series Edit — SPECIFIED & USER-APPROVED 2026-07-16, Design next.** Architecture was already converged & LOCKED (research.md D1–D6). This session closed the open Discuss questions → D7 (all three scopes ship in v1, named in **English**: "This one" / "This and following" / "All"), D8 (installment k/N position-anchored), D9 (bulk edits clobber prior single edits in v1; override-preservation deferred), **D10 (changing an installment's total count is only allowed at "All" scope → no mixed 4/12 + 5/18 labels; chosen for predictability)**. Spec has 12 reqs: SEDIT-01..10 = P1 (RecurrenceDefinition entity + one-per-seriesId migration backfill, three-scope attribute edits [amount/description/category/split, NO range], SPLIT-01 preserved, per-occurrence auth fail-closed, retire legacy `frequency`/B-001, FE scope-chooser + prefilled edit form + read endpoint); SEDIT-11..12 = P2 (range resize: installment count change under "All" only with position-anchored relabel, recurring end-date change). **USER FEEDBACK (durable): the whole system is English-only, no exceptions** — saved as memory `english-only.md`; watch for Portuguese leaking from discussion into artifacts. Code map confirmed (agent, 2026-07-16): no metadata entity today (series = N rows sharing UUID `seriesId`; template discarded post-generation); only POST/DELETE `/series` + single-row PUT exist (no edit endpoint/UI); generator loops `parcel=currentParcel..parcelsNumber`, label `"desc (k/N)"`, MAX_OCCURRENCES=120; deleteSeries loads by `(plan,seriesId)` + authorizes each; **Flyway highest V7 → next V8**. NOTE the earlier "series-edit PAUSED at Specify/Discuss" Deferred-Ideas entry is now superseded by this spec. — Prior Round-1 entry below.
- Prior: **Filters/Sorting/Split-Correctness (Round 1) — ✅ EXECUTED & E2E-VERIFIED 2026-07-16. All 14 tasks (B1–V) done; 15/16 requirements Verified (SORT-04 endDate deferred, non-blocking).** Backend `ae689b2`→`342a8b6` (B2–B7) + frontend `f9c3357`→`486ef45` (F1–F6), all on `main` in both repos (working trees clean). B1 integrity gate passed (258 tx, 0 orphans, 0 sum-mismatches). Full-stack E2E against a copy DB (`dev_finsight_verify`, dropped after) passed every check: AGG-02 regression (unfiltered income 85067.68 / expense 31741.30 identical old-vs-new), DASH partition (Lucas 17020.24 + Livia 14721.06 = 31741.30 across summary + all 15 categories), FILT (mem1=146/mem2=129/all=258/non-member=0), SRCH-01 (name search 129, no dupes via EXISTS), SORT-02 (category nulls-last), SORT-03 (**largest-share correlated-subquery ORDER BY executes HTTP 200, monotonic across page boundary — no native fallback needed**). Real `dev_finsight` never written by this work (it grew 258→266 from the user's own concurrent app activity — tx 421–428, `created_by=1`). **Only remaining: human visual pass of the FE pickers + sort UI (build-green ≠ runtime-correct for Base UI, STATE L-002).** Full detail in `.specs/features/filters-sorting-split/tasks.md` Progress Log. — Prior planning note below.
- Prior: **Filters/Sorting/Split-Correctness (Round 1) — SPECIFIED + TASKED + HANDED OFF 2026-07-16 (Design skipped — no new pattern, user-confirmed). Tasks APPROVED; ready to execute in a fresh session via `.specs/features/filters-sorting-split/handoff-execute.md` (say "resume work").** Spec at `.specs/features/filters-sorting-split/spec.md`; tasks at `.specs/features/filters-sorting-split/tasks.md` (14 tasks: B1 integrity gate, B2–B7 backend, F1–F6 frontend, V E2E). **No migration this round** (participants/share_amount from V6 already exist — query/param/UI only). Two risk flags: B1 (verify SPLIT-01 invariant on live data BEFORE flipping aggregation in B6 — if any orphan/mismatched tx, STOP and backfill first) and B5 (largest-share attributed-to sort — correlated subquery in the Specification; native-query fallback documented). Spec has 16 reqs: FILT-01..03, DASH-01..05, AGG-01..03, SRCH-01, SORT-01..04. Scope: single-select user filter (attributed-to/participant semantics) on the transactions table + dashboard; make all dashboard widgets aggregate `share_amount` (honors the user filter; unfiltered totals unchanged thanks to the SPLIT-01 invariant); user-aware search (match participant name); fix sorting (single active sort key — the `sorting[0]`-only indicator/ordering mismatch — plus Category and Attributed-to-by-largest-share sortable). Key insight: given SPLIT-01 (every tx has participations summing to amount), participant-based aggregation does NOT change unfiltered dashboard numbers — its value is a correct per-user filter; AGG-03 must verify that invariant on live data before the switch. User decisions (2026-07-16): dashboard-filtered = that user's shares only; category limit stays plan-total when filtered ("simpler for now", revisit Round 2); sort-by-user = largest-share participant; table filter single-select + full amount in row. Out of scope → Round 2: month prev/next nav, projected spending, dedicated per-user-per-category widget. Prior housekeeping entry below.
- Prior: **Housekeeping pass (2026-07-16): cleared two dangling items found after Expense Items P1.** (1) The pre-existing uncommitted `applyParticipants` reconcile-by-key fix in `FinancialTransactionService.java` was reviewed, compile-verified, and committed (`05b5aca`) — backend working tree is now fully clean. (2) Recurring & Installment Transactions' two long-paused runtime E2E passes (T12, RECUR-10 T17) were finally run — see the "Previous Work" entry below for results (21/21 passed). Both repos are clean and all previously-known work is either shipped-and-verified or explicitly deferred (Expense Items P2/P3). Next up: user-requested refinements (transaction filter/sort by user, verifying split-transaction dashboard reflection, dashboard improvements) — not yet started.
- Previous: **Transaction Line Items ("Expense Items") — P1 (T1–T10) EXECUTED & VERIFIED 2026-07-15. T14 closed for this pass.** All 10 P1 tasks implemented, gated green, and committed on `main` in both repos (backend `2edf5ee`..`e475680` covering T1/T2/T3/T4/T6/T7/T5/T8; frontend `a2bfaf1`, `301ae87` covering T9/T10). T14's backend half passed 24/24 API-driven checks against a **throwaway copy DB** (`dev_finsight_verify`, never the real `dev_finsight`) — itemized breakdown math, non-itemized regression, all validation guards (overflow/negative/blank/category-not-in-plan/type-mismatch), update full-replace, inline-lower-below-Σitems guard, and the uncategorized-parent case all correct. The FE visual pass (form add/remove/remainder/submit) was **handed to the user to validate manually** (2026-07-15) after the Postgres SSH tunnel dropped twice during an agent-driven headless-Chromium attempt — user's call, not a blocker. Full detail in `.specs/features/expense-items/tasks.md` Progress Log. **Next**: P2 (T11 quantity, T12 table indicator) + P3 (T13 series items), still deferred to a follow-up pass.
- Previous: **Expense Splitting & Attribution (Fase B of Shared Plans) — ALL 13 TASKS EXECUTED & E2E-VERIFIED 2026-07-13, requirements SPLIT-01..08 shipped.** Backend `a0a5c39`..`56f5dd5` (T1–T9) + `56f5dd5` recurring; frontend `0c1271c`..`0b16912` (T10–T12), all on `main` (both repos still unpushed — 18+ backend / 8+ frontend local-only commits from before, now more on top). See `.specs/features/expense-splitting/` for spec/design/tasks — tasks.md has the full Progress Log + T13 E2E results (21/21 checks passed).
- Previous: Shared Plans (M4) — P1/MVP DONE & UAT-passed. Part 2 (P2/P3, PLAN-07..11, T34–T52) EXECUTED & VERIFIED 2026-07-12 — all 11 requirements (PLAN-01..11) shipped.** (Backend Phases 1–5 `795a079`..`0d20fa3` + Frontend Phase 6 `99fe622`..`a00966b`; Part 2 backend `7eb2f4f`..`9316af4` + frontend `ef14903`..`6957e02`, all on `main`.)
- Flyway adopted (V1 baseline, V2 plan tables, V3 destructive re-scope) — real dev DB migrated to v3, data-preserving (dry-run on a copy + real-DB assertions all passed: 3 users/51 tx/21 cat intact, 3 default plans, `created_by` backfilled, `user_id` dropped).
- Plan domain (Plan/PlanMembership/PlanInvitation + enums + repos + PlanService + PlanController), two-layer authz (PlanContext resolver reads `/plans/{planId}` path var + PlanAuthorization role×row-ownership, 20 unit tests), invitations (email+link, accept idempotent).
- **Full backend E2E passed** (invite email+link accept; access matrix all cells server-side: CONTRIBUTOR edits only own, VIEWER read-only, OWNER manages, non-member 404). All test data cleaned up.
- **How to run/verify:** boot with `SERVER_PORT=3099` (avoid the dev :3000); env from `backend/.env` (local Postgres `dev_finsight`, user `dev`). ddl-auto=validate; Flyway owns schema. Postgres has been flaky (went down mid-session once) — if boot fails with "Connection refused", start it (`sudo systemctl start postgresql`).
- **Frontend DONE** (Phase 6, T27–T32, commits `99fe622`..`a00966b` on finsight-frontend `main`; RECUR-10 FE work committed separately as `c387ebb`): plan DTOs, usePlanService, PlanProvider (activePlanId+localStorage+default-plan seeding), data hooks rewired under `/plans/${activePlanId}/`, plan switcher + management UI, invitation service + accept page. `npm run build` green.
- **Full-stack verified up**: backend :3000 + vite :5173 talking. Frontend expects backend at `localhost:3000` (VITE_FINSIGHT_API_URL).
- **Remaining:** ONLY the visual UAT (T33) — human walk-through of the plan UI (switcher/create/invite/accept). Backend access-matrix + invite E2E already passed via API.
- **Part 2 (P2/P3, PLAN-07..11) — EXECUTED & VERIFIED 2026-07-12.** All 18 tasks (T34–T51) implemented across 5 independent phases + T52 full API-driven E2E, all against the live dev DB reached via SSH tunnel (local Postgres was down for this session). New: `CategoryBreakdownDto`/`PersonBreakdownDto` dashboard evaluation fields, member role-change/removal, plan rename/soft-delete (`V4` adds `deleted_at`)/leave/transfer-ownership, invitation revoke+expiry (410). No bugs found during E2E. Flyway is now at V4.
- **Test-data note:** a throwaway UAT user `uat_1783836782@t.com` / `pw123456` (userId 10, plan 12) was left in the dev DB for convenience during T33; clean up after UAT. Real data remains: users 1–3, 51 tx, 21 cat, their 3 default plans (Part 2's own throwaway test users/plans were fully cleaned up by T52, verified byte-for-byte against this baseline).
See AD-004 (model), AD-005 (path transport), AD-006 (soft delete).

**Planning artifacts (done):** spec.md + context.md + design.md + tasks.md in `.specs/features/shared-plans/` — tasks.md has a live Progress log with all commit hashes.

**Previous Work: Recurring & Installment Transactions — ALL TASKS (T1–T17) COMPLETE & E2E-VERIFIED 2026-07-16.** Core (T1–T11) + T12, and RECUR-10 refinement (T13–T16) + T17 all passed 21/21 API-driven checks against a throwaway copy DB — installment/recurring series creation, dashboard look-ahead, series/single delete, and in-progress installments (current-parcel `k`) all correct. No bugs found. See `.specs/features/recurring-transactions/tasks.md` for the full results.

---

## Recent Decisions (Last 60 days)

### AD-001: Adopt spec-driven development (tlc-spec-driven) on finSight (2026-07-05)

**Decision:** Install the `tlc-spec-driven` skill into `finsight/.claude/skills/` and manage all future work through the `.specs/` tree.
**Reason:** Project is being resumed after a gap; wanted structure/traceability that wasn't used at the start.
**Trade-off:** Upfront documentation effort before writing feature code.
**Impact:** Brownfield docs, PROJECT/ROADMAP, and per-feature specs now precede implementation.

### AD-002: Not a forecast — deterministic transaction generation (2026-07-05, supersedes the projection framing)

**Decision:** Instead of a statistical forecast, the feature generates concrete future `FinancialTransaction` rows from a bounded recurrence/installment definition. The existing dashboard shows the look-ahead for free. No averages, no projection math — "just use what's registered." v1 requires a start AND end (no infinite recurrence). Occurrences share a `seriesId`. Feature renamed spending-forecast → recurring-transactions.
**Reason:** User reframed at the spec checkpoint: they want to register "expense X, 12 parcels" and have 12 real transactions generated, not a computed projection. Simpler, explainable, and dashboard-compatible.
**Trade-off:** Variable/one-off spending is not anticipated at all (only registered commitments appear); bulk rows instead of a lightweight computed view.
**Impact:** Reuses existing `frequency`/`parcelsNumber`/`startDate`/`endDate`; adds a `seriesId` column (via `ddl-auto=update`). No dashboard changes needed.

### AD-003: In-progress installments via a "current parcel" input (2026-07-11)

**Decision:** Refine RECUR-10 so an installment can be registered mid-way. Input = total `N` + **current parcel `k`** (default 1); the generator produces parcels `k..N` **inclusive** (`N − k + 1` rows), the `startDate` is the month of parcel `k`, and descriptions carry the real `k/N..N/N` numbers. `k = 1` reproduces the original `1/N`-from-start behaviour exactly (backward compatible). The implied end is shown **read-only** in the form (no separate end-date input — that would duplicate `N` and re-open the count-vs-end conflict). Installments only; recurring is untouched.
**Reason:** The original generator always assumed the first parcel, so a purchase the user is already several parcels into either got wrong labels or double-counted parcels already imported from the Nubank CSV. User chose the "current parcel" framing (over "next" / "already paid") at the refinement checkpoint.
**Trade-off:** No automatic dedup against CSV imports — avoiding double-counting relies on the user setting `k` to the first not-yet-imported parcel. New `currentParcel` field threaded through DTO → generator → form.
**Impact:** Adds `currentParcel` to the series request DTO (backend + frontend), changes `generateInstallments` to a `k..N` loop with cap on the generated count, adds a service `1 ≤ k ≤ N` guard, and a form current-parcel input + read-only computed end. No schema change (no new column). Tasks T13–T17.

### AD-004: Shared Plans model replaces the "household" framing (2026-07-11)

**Decision:** Design the M4 Share milestone as **Shared Plans** — a spreadsheet-style shareable container — instead of the original "household / merge two users into one scope" framing. A `Plan` owns transactions and categories; a user belongs to many plans via `PlanMembership` with a role (OWNER | EDITOR | CONTRIBUTOR | VIEWER). Access is **two-layer**: plan role × row-level ownership (`created_by`) — a Contributor edits only their own rows, everyone sees everything, only the Owner manages members/categories/plan. Categories are shared at the plan level with a plan-total spending limit; the dashboard is combined + per-person. Every user gets an auto-created personal plan and their existing data migrates into it. Invites work by email and by link. Specced in `.specs/features/shared-plans/` (spec.md + context.md); ROADMAP M4 updated.
**Reason:** At the sharing-model checkpoint the user described "sharing like an Excel spreadsheet" with finer-grained access (a person adds/edits their own things but not the rest). The household model can't express privacy, read-only, or partial sharing; the container model subsumes it (a two-person shared plan _is_ a household) and adds those.
**Trade-off:** More entities (Plan, PlanMembership, PlanInvitation) and a more involved authorization layer than a single household flag. The auto-created default plan is a normal, **shareable** plan (user decided 2026-07-11 — no private/immutable "personal" space); only guard is a user can't delete/leave their last plan.
**Impact:** Re-scopes `financial_transactions` and `financial_transaction_categories` from `user_id` to `plan_id` + `created_by`. **Hard prerequisite: M3 schema migrations (Flyway/Liquibase)** — the re-scope is destructive and cannot run on `ddl-auto=update`. **User chose to do Shared Plans FIRST** (they need multi-person expense entry now), deferring the per-transaction items feature; the two are independent. Consequence: the M3 migration foundation must be the first step of this track (adopt Flyway + baseline before the re-scope). Next phase: Design (PLAN-01..06 are the P1/MVP set).

### AD-005: Shared Plans — active-plan transport via path variable `/plans/{planId}/...` (2026-07-12)

**Decision:** Plan-scoped requests carry the active plan as a URL **path variable** (`/plans/{planId}/financial-transaction`, `.../financial-transaction-category`, `.../dashboard`), resolved server-side into a `PlanContext(plan, user, role)` by a `PlanContextArgumentResolver` that reads the URI template var and verifies membership. Plan-**management** endpoints (create/list plans, invitations) stay user-scoped and are not nested. The plan is deliberately kept **out of the JWT** (email-only subject unchanged) so switching plans needs no re-login.
**Reason:** User chose the path approach over an `X-Plan-Id` header after a trade-off discussion, prioritizing "do it the standard/explicit way" while learning. Path scoping is structural — it can't be silently forgotten (no cross-plan leak) — and follows mainstream resource-hierarchy REST (GitHub-style). Header was the lower-churn alt but relies on discipline and hides the context.
**Trade-off:** Rewrites the existing flat transaction/category/dashboard routes under a `/plans/{planId}` prefix (cheap now, small codebase). Frontend service hooks build URLs under `/plans/${activePlanId}/...` and include `activePlanId` in TanStack Query keys (switch = natural refetch).
**Impact:** `ApiRoutes` gains a plan prefix for scoped routes; controllers swap `User loggedUser`-scoping for a `PlanContext` param (`@AuthenticationPrincipal` still identifies the actor for row-ownership/attribution). Recorded in `design.md` (Tech Decisions). Design phase essentially complete; next is Tasks.

### AD-006: Plan deletion is a soft delete (2026-07-12)

**Decision:** PLAN-10's "delete plan" is a **soft delete** — add a nullable `plans.deleted_at` (Flyway `V4`) and `Plan.deletedAt`; deleting sets the timestamp and archives the plan for all members. Dependent rows (transactions, categories, memberships, invitations) are **never cascaded/destroyed**. Archived plans are filtered out at the read paths (list-my-plans, `PlanContextArgumentResolver`, login-default, and the last-plan guard) via **explicit per-finder `deleted_at IS NULL`** conditions — deliberately NOT a global Hibernate `@SQLRestriction`, which would break `membership.getPlan()` association loads for a soft-deleted plan. Atomized as T42 (foundation) + T43 (rename+soft-delete) in tasks.md.
**Reason:** User flagged a hard cascade delete as too risky/unprofessional. Soft delete makes deletion reversible, keeps an audit/history trail, and defers the truly destructive operation.
**Trade-off:** A schema column + a handful of filtered finders (vs. zero-churn hard delete). Archived rows accumulate until a future purge exists.
**Impact:** Replaces the earlier "service-level cascade vs `ON DELETE CASCADE`" open question (now moot). Permanent deletion + retention is deferred (see Deferred Ideas).

### AD-007: Expense Items — item-level category attribution via a partition invariant (2026-07-15)

**Decision:** Line items are an **additive** child of `FinancialTransaction` (`TransactionItem`, mirroring `TransactionParticipant` — `@OneToMany` cascade+orphanRemoval, migration V7, no backfill). Each item carries its **own** category + amount. The dashboard **category breakdown** becomes item-aware via a **Partition Invariant**: a transaction's `amount` is sliced across category buckets exactly once — categorized items → their own category; uncategorized items + the un-itemized remainder → the parent transaction's category. Sums are **loose** (`0 ≤ Σitems ≤ amount`; overflow rejected), so the remainder bucket is always ≥ 0. Computed as `spent[C] = A[C] − B[C] + I[C]` (A = `SUM(ft.amount)` by parent cat = today's query; B = categorized-item sums by parent cat; I = categorized-item sums by item cat), extracted into a **pure `CategoryBreakdownAssembler` with unit tests**. Items and participants are **independent axes** (items = *what*, participants = *who owes*); **no new authz gate** (reuse transaction modify/create guards); update is **full-replace** (no business key). Applies to **both** CREDIT and DEBIT. **No "Uncategorized" bucket** in v1 — a category-less transaction's remainder stays out of the breakdown (as today), but a categorized item on it still surfaces.
**Reason:** The driving need is "cleaning vs. food inside one grocery run" — answerable only if the item is the unit of category truth. Loose sums avoid forcing the user to itemize taxes/freight; the remainder has a defined home on the parent category. The `A−B+I` decomposition turns the only hard query into three plain aggregations.
**Trade-off:** Rewrites the one category-keyed dashboard query (`buildCategoryBreakdown`) and touches PLAN-07 limit evaluation downstream; more entities than a flat "notes" field. **Top-line totals (income/expense/net) provably never move** — they don't read categories.
**Impact:** Specced/designed/tasked in `.specs/features/expense-items/` (ITEM-01..14, 14 tasks). No code yet. First execution pass = P1 (T1–T10); P2/P3 deferred.

---

## Active Blockers

### B-001: `frequency` is free-text (no controlled vocabulary)

**Discovered:** 2026-07-05
**Impact:** The generation feature needs a defined set of intervals; the existing `frequency` String has no controlled vocabulary. Lower severity now — generation defines its own recurrence input rather than trusting legacy free-text values.
**Workaround:** The feature's create form defines the allowed intervals (monthly/installments for v1); legacy free-text values are not relied upon for generation.
**Resolution:** Design defines the v1 interval set; a first-class `frequency` enum is tracked as an M3 feature.

### B-002: Login with an unrecognized email returned 500, not 401 (found 2026-07-17) — ✅ FIXED 2026-07-19 (`b23d940`)

**Discovered:** 2026-07-17, by `AuthenticationIT.loginForUnknownEmailIsRejected` during test-foundation T12.
**Impact:** `POST /auth/login` with an email that has no matching user threw `InternalAuthenticationServiceException` wrapping `UsernameNotFoundException` inside Spring Security's auth manager. `GlobalExceptionHandler` maps `BadCredentialsException` (wrong password) to 401 but had no handler for this wrapped exception, so it fell through to the generic handler → HTTP 500. A wrong password correctly returned 401; an unknown email incorrectly returned 500 — inconsistent, and (the real risk) the differing response let a caller **enumerate registered emails**.
**Resolution:** Fixed 2026-07-19 (`b23d940`). Root cause: `CustomUserDetailsService` threw a *custom* `UserExceptions.UsernameNotFoundException`, which `DaoAuthenticationProvider` did not recognize as a user-not-found (so it wasn't hidden/converted). Changed it to throw Spring Security's own `UsernameNotFoundException`; `hideUserNotFoundExceptions` (on by default) now converts it to `BadCredentialsException` → the existing 401 handler with generic "Invalid username or password." body. Unknown-email and wrong-password are now byte-identical in status AND body (new `AuthenticationIT.unknownEmailAndWrongPasswordAreIndistinguishable` test proves it). Removed the now-dead custom exception + its 404 handler mapping. `./mvnw clean verify` green (55 unit + 59 integration).

### B-003: Negative top-level transaction amount was accepted, not rejected (found 2026-07-17) — ✅ FIXED 2026-07-19 (`c62e7be8`)

**Discovered:** 2026-07-17, by `TransactionCrudIT` during test-foundation T13.
**Impact:** `FinancialTransactionRequestDto.amount` carried only `@NotNull` (no `@Positive`); a PUT/POST with `amount: -50.00` and no line items succeeded (200) and persisted the negative value, silently skewing dashboard totals. Item-level amounts were already guarded.
**Resolution:** Fixed 2026-07-19 as part of the consistency-review commit `c62e7be8`: added `@Positive(message = "Amount must be positive.")` to `FinancialTransactionRequestDto.amount`, mirroring the item-level guard. The IT was updated to assert rejection (`putWithNegativeTransactionAmountIsRejected`). Suite green.

---

## Lessons Learned

### L-001: Verify the task ledger against reality at phase boundaries (2026-07-05)

**Context:** During Execute, tasks T6 (service `createSeries`/`deleteSeries`) and T7 (controller endpoints) were briefly marked Done in tasks.md before being implemented.
**Problem:** The backend still compiled (what existed was consistent), so "compiles" masked the fact that the core wiring didn't exist yet — the feature would have been non-functional.
**Solution:** Re-checked the launched-agent ledger against the task list, caught the gap, implemented T6+T7, re-gated.
**Prevents:** Treating a green compile as proof of completeness. Compile ≠ coverage; always reconcile executed tasks against the plan before declaring a phase done.

---

### L-002: Frontend build-green ≠ runtime-correct for Base UI context (2026-07-12)

**Context:** During Shared Plans UAT, the plan switcher threw `Base UI: MenuGroupRootContext is missing` at runtime, though `npm run build` (tsc + vite) had passed clean.
**Problem:** Base UI compound components (e.g. `Menu.GroupLabel` needing a `Menu.Group` ancestor) enforce their parent-context requirement at RUNTIME, not in the type system — so a missing wrapper compiles fine and only fails on render/interaction.
**Solution:** Wrapped `DropdownMenuLabel` + the radio group in `DropdownMenuGroup` (the pattern the component's own `.stories.tsx` uses). Fixed in `07b5e92`.
**Prevents:** Trusting a green FE build as proof a component renders. For Base UI / compound-component UI, a visual/interaction pass (or a story with `play`) is required — mirror the primitive's own story for correct nesting.

---

### L-004: Re-verify migration numbering against reality at the start of Execute, not just at planning time (2026-07-13)

**Context:** Expense Splitting's design.md/tasks.md (written 2026-07-12) assumed the feature's migration would be `V5__add_transaction_participants.sql`. By the time a fresh session executed T1, an unrelated `V5__translate_default_plan_names.sql` had already landed on `main` (a same-day i18n fix), so V5 was taken.
**Problem:** Planning artifacts capture repo state as of the planning session; if Execute runs in a later session (by design — "planner doesn't execute", see [[execution-in-separate-chat]]), the repo may have moved on in the interim.
**Solution:** Before T1, checked `ls db/migration/` against the plan's assumed filename, found the collision, renumbered to V6, and recorded a `SPEC_DEVIATION` note at the top of tasks.md rather than silently adjusting.
**Prevents:** Assuming a spec/design/tasks artifact's file-numbering, line numbers, or "next available X" claims are still accurate — always re-check against the current repo state as the first step of Execute, and log any drift explicitly.

### L-003: Local Postgres isn't always the DB — check for a tunnel before declaring "DB unavailable" (2026-07-12)

**Context:** Starting Part 2 execution, `pg_isready`/`systemctl` showed local Postgres inactive and unstartable without a sudo password.
**Problem:** Assumed the DB was simply down and planned to defer DB-dependent tasks (schema migration boot-verify, runtime E2E).
**Solution:** The user reconnected an SSH tunnel (port-forwarded `localhost:5432` to the real dev/remote instance via an active `ssh` process) — `ss -tlnp | grep 5432` showed the tunnel, and `pg_isready` then succeeded. No local Postgres service is actually used in this workflow.
**Prevents:** Before concluding "the database is unreachable," check `ss -tlnp | grep 5432` for an existing SSH-tunneled listener, not just `systemctl status postgresql` — the two are unrelated and only the tunnel matters here.

### L-005: The SSH tunnel's local listener staying up does NOT mean the connection is alive — always probe with a timeout (2026-07-15)

**Context:** During Expense Items T14 E2E, `ss -tlnp | grep 5432` showed the tunnel's local listener present and the `ssh` process alive throughout, but `psql`/Spring Boot boot attempts hung or failed with "connection has been closed" — twice in the same session, including right after the user reconnected the tunnel.
**Problem:** `ss`/`ps` only prove the local end is listening and the process hasn't exited; they say nothing about whether the remote transport is actually alive. A plain `psql ... -c "SELECT 1"` with no timeout can hang indefinitely on a half-dead tunnel, wasting time before the problem is even noticed.
**Solution:** Always wrap connectivity probes in `timeout N` (10-15s) and treat exit code 124 as "tunnel is stale," distinct from a clean connection-refused. When it recurs after a user-reported reconnect, don't keep silently retrying — surface it and ask whether to keep waiting or hand off, rather than burning turns on an infrastructure problem outside the agent's control.
**Prevents:** Trusting `ss -tlnp`/process-alive checks as proof of DB reachability; burning time on unbounded `psql` calls against a flaky tunnel instead of a quick timeout-guarded probe.

### L-006: A dev server on a non-default port needs its port added to the backend's `ALLOWED_ORIGINS`, or CORS silently fails as "An error occurred" (2026-07-15)

**Context:** For Expense Items T14's FE visual pass, Vite was started on `:5183` (to avoid the user's own dev server already running on `:5173`) pointed at a throwaway backend on `:3099`. Registration failed in the browser with a generic toast ("An error occurred") and no other symptom.
**Problem:** `SecurityConfig.corsConfigurationSource()` reads `ALLOWED_ORIGINS` as a literal comma-separated allow-list (`.env` ships only `http://localhost:5173`); a request from any other origin is silently blocked by CORS, and the frontend surfaces this as a generic network-error toast, not a CORS-specific message.
**Prevents:** When running a throwaway frontend instance on a non-default port against a throwaway backend instance, export `ALLOWED_ORIGINS` for that backend instance to include the exact `http://localhost:<port>` used — don't assume the default `.env` value covers it. A generic "An error occurred" toast on register/login in a throwaway E2E setup is a CORS-origin-mismatch smell before anything else.

### L-007: A migration file on disk can get auto-applied to the real DB before it's even committed, if another process boots against the same database (2026-07-16)

**Context:** During series-edit's T12 pre-flight, `dev_finsight`'s `flyway_schema_history` showed V8 already `installed_on` 2026-07-16 21:47:09 — a minute before this session committed the V8 migration file (21:48:30).
**Problem:** Spring Boot runs Flyway automatically on every application boot. The migration file existed on disk (written by an Execute sub-agent) before it was committed; the user's own separately-running dev backend instance auto-reloaded/restarted in that window and picked it up, applying it straight to the real DB — bypassing the planned copy-DB verification gate entirely. No agent command caused this.
**Solution:** Verified safety via read-only checks against the real DB directly (definition-per-series 1:1 count, zero unlinked FKs, zero SPLIT-01 invariant violations, spot-checked backfill correctness) — these checks are strictly stronger evidence than a copy-DB check would have been, since they're against the actual production-adjacent data. Informed the user immediately rather than silently proceeding; they confirmed treating this as satisfying the integrity gate.
**Prevents:** Assuming a migration file is inert until `git commit` or until *this session* boots the app — if a dev database is shared with another running instance (an IDE run config, a user's own `npm run dev`-equivalent backend, hot-reload), a migration file merely existing on disk is enough to trigger auto-apply the moment ANY app instance restarts against that DB. When starting a copy-DB verification pass, check `flyway_schema_history` on the REAL DB first (not just the copy) to catch this before assuming a clean "pre-migration" baseline exists.

### L-008: This machine's Docker Desktop needs two non-default env vars before Testcontainers will boot (2026-07-17)

**Context:** Standing up test-foundation's T6 harness smoke IT, `./mvnw verify` failed through four distinct errors in sequence before Testcontainers actually worked: (1) `docker info` succeeded but Testcontainers said "Could not find a valid Docker environment" — the active Docker context (`desktop-linux`) uses socket `~/.docker/desktop/docker.sock`, not the `/var/run/docker.sock` Testcontainers checks by default; (2) even with `DOCKER_HOST` pointed at `docker.sock`, every strategy failed — Docker Engine 29's daemon requires API ≥1.44, but testcontainers 1.21.x's bundled docker-java always probes with a hardcoded API 1.32 first (upstream bug, fixed in testcontainers 2.x: https://github.com/testcontainers/testcontainers-java/issues/11210); (3) after pinning the API version, `docker.sock` itself turned out to be a path-translating proxy — starting the Ryuk cleanup container failed with "mounts denied: /socket_mnt/... is not shared from the host" because that host path isn't in Docker Desktop's file-sharing allowlist; (4) switching `DOCKER_HOST` to `docker.raw.sock` (talks directly to the VM's dockerd, no path translation) fixed the mount error, but Ryuk's own container then failed to start/was immediately not-found.
**Problem:** None of these are code defects — they're this specific Docker Desktop-on-Linux installation's socket topology and a real upstream version-negotiation bug. Without knowing the exact fix, each symptom looks like "Docker isn't set up," which would wrongly block the whole test-foundation feature.
**Solution:** Three things, together, make `mvn verify` boot the harness on this machine: (a) `backend/src/test/resources/docker-java.properties` with `api.version=1.44` (committed — this is a real fix for anyone on Docker Engine 29+, not machine-specific); (b) `export DOCKER_HOST=unix:///home/lcs/.docker/desktop/docker.raw.sock` (machine-specific, NOT committed); (c) `export TESTCONTAINERS_RYUK_DISABLED=true` (machine-specific — Ryuk's container never starts cleanly here, but a plain JVM shutdown hook still removes the Postgres container on normal exit, confirmed via `docker ps -a` after a run). All three must be set together for `./mvnw verify` to reach the Testcontainers-backed `*IT` tests.
**Prevents:** Re-diagnosing this from scratch in a future session. If `./mvnw verify` fails with "Could not find a valid Docker environment," "client version ... too old," or "mounts denied," export `DOCKER_HOST` and `TESTCONTAINERS_RYUK_DISABLED` as above before assuming Docker itself is broken. Documented for future sessions/users in `TESTING.md` (T21).

---

## Quick Tasks Completed

| #   | Description | Date | Commit | Status |
| --- | ----------- | ---- | ------ | ------ |
| —   | —           | —    | —      | —      |

---

## Deferred Ideas

- [x] **M3 Test Foundation — EXECUTED & E2E-VERIFIED (2026-07-17)**, spec+design+tasks in `.specs/features/test-foundation/`. Testcontainers backend harness (10 `*IT` classes) + JaCoCo floor + frontend `unit` vitest project + Base UI interaction guard. See Current Work entry above for full results. Found (not fixed) B-002 and B-003.
- [ ] **Recurrence Model v2 (`frequency` enum)** — replace the legacy free-text `frequency` field with a controlled vocabulary (B-001); not started. Split out from the old "test foundation" roadmap grouping into its own M3 forecast-track item, 2026-07-17.
- [ ] **Move `*IT` off Testcontainers onto the local `dev_finsight` DB** (retire the L-008 Docker env-var boilerplate) — sequenced pipeline, do in order: (1) migrate data from `dev_finsight` (currently holds the real/prod data — it was used as prod before the real prod DB existed) → the new empty prod DB; (2) once `dev_finsight` is drained of real data, repoint the 10 `*IT` classes at it via `application-test.properties` (it becomes the throwaway test DB — there are only two banks, no separate test DB); (3) then clear the Docker stuff if no longer needed — delete `TestContainersConfig.java` + `docker-java.properties`, drop the `DOCKER_HOST` / `TESTCONTAINERS_RYUK_DISABLED` env vars (L-008). **⚠️ Hard constraint:** `AbstractIntegrationTest.truncateAll()` runs `TRUNCATE ... CASCADE` on every table before each test. Step 2 is safe ONLY after step 1's migration is verified complete — until then `dev_finsight` is the sole copy of the real data, and pointing tests at it would wipe it. NEVER point tests at the prod DB. Until this is done, day-to-day is `./mvnw test` (unit only, no Docker); `./mvnw verify` is deferred. Captured 2026-07-24.
- [x] **Fix B-002** — ✅ FIXED 2026-07-19 (`b23d940`): login now returns identical 401 for unknown-email and wrong-password (throws Spring's `UsernameNotFoundException` so it's hidden→`BadCredentials`); closes the user-enumeration leak. See B-002 in Active Blockers above.
- [x] **Fix B-003** — ✅ FIXED 2026-07-19 (`c62e7be8`): `@Positive` added to `FinancialTransactionRequestDto.amount`. See B-003 in Active Blockers above.
- [x] **Human visual UAT of Expense Splitting FE (T11 titular column + T12 split form)** — user confirmed done (2026-07-13), no issues reported.
- [ ] **Server-sent invite emails** — the email invite currently only records the invitation + returns a token (owner shares the link manually via the frontend, which builds the URL from `window.location.origin`). If/when the backend actually sends the email, it needs a configured `FRONTEND_BASE_URL` env var (no `window.location` server-side). Captured during: Shared Plans UAT (2026-07-12).
- [x] **Shared Plans P2/P3 — EXECUTED & VERIFIED (2026-07-12)**, tasks **T34–T52** in `.specs/features/shared-plans/tasks.md` ("Part 2" section); spec traceability now "Verified". Five independent, individually-shippable slices, all shipped: PLAN-07 category-limit evaluation, PLAN-08 per-person dashboard by `createdBy`, PLAN-09 manage members, PLAN-10 plan lifecycle rename/leave/transfer/**soft-delete** (see AD-006), PLAN-11 invite revoke/expiry. T52 (full API-driven E2E against the live DB) passed with zero bugs found.
- [ ] **Permanent plan deletion + retention** — soft delete (AD-006) leaves archived plans + their rows in the DB indefinitely. Future: a way to permanently purge an archived plan (owner-triggered "delete forever" and/or a scheduled retention job), plus possibly an "restore archived plan" affordance. Captured during: Part-2 planning (soft-delete decision, 2026-07-12).
- [x] **Expense items P1 (T1–T10) — EXECUTED & E2E-VERIFIED (2026-07-15)**, spec+design+tasks in `.specs/features/expense-items/`. Additive `TransactionItem` child, item-aware category breakdown, FE items form section. Backend E2E 24/24; FE visual pass handed to user for manual validation.
- [ ] **Expense items P2** — quantity input wired end-to-end (T11), itemized indicator + item view in the transactions table (T12). Captured during: Expense Items design, 2026-07-14.
- [ ] **Expense items P3** — items replicated across recurring/installment series occurrences (T13); each generated occurrence would carry a copy of the same items. Currently, series occurrences generate with **zero items** even if the create-series form had items filled in — a real gap to flag during manual testing. Captured during: Expense Items design, 2026-07-14.
- [ ] Open-ended / infinite recurrence via rolling-window generation ("Google Calendar forever") — v1 requires an end; Captured during: Recurring Transactions spec
- [ ] Alerts when balance goes negative / a category limit is projected to break — Captured during: Recurring Transactions spec
- [ ] Bulk/smart series edit (change amount or range, apply to future occurrences) — P3 of the feature; Captured during: Recurring Transactions spec
- [x] **Edit series (+ recurrence refactor) — EXECUTED & E2E-VERIFIED 2026-07-16 (`.specs/features/series-edit/`), SEDIT-01..12 all Verified.** See the Current Work entry above for full results. Historical draft-direction notes retained for context:
- [~] **Edit series (+ recurrence refactor) — was PAUSED at Specify/Discuss (2026-07-16); now planned (see above).** Today a series has NO first-class entity: it's N `FinancialTransaction` rows sharing a `seriesId` (UUID); the recurrence definition (mode/interval/parcels/range/split template) lives only in the create-request DTO and is discarded after generation. `frequency` is still legacy free-text (blocker B-001) coexisting with the `RecurrenceMode`/`RecurrenceInterval` enums. **Draft direction (leanings, NOT locked):** user leaned toward (a) *user chooses scope per edit* — this / this+future / all (Google-Calendar style), and (b) *allowing range changes* (parcels/end-date → add/remove occurrences). Both push toward introducing a **`RecurrenceDefinition`/`Series` entity** (edit = update definition + regenerate future occurrences), which also cleans up the `frequency` legacy and needs a **migration of existing series data**. Open questions still to close in Specify/Discuss: editable fields set; how range change diffs/relabels (k/N); whether a manually-edited single occurrence is preserved vs overwritten; recurring (open-ended) vs installment edit differences. **Research DONE (2026-07-16) → `.specs/features/series-edit/research.md`.** Key findings: two canonical models — calendar (master+RRULE, compute-on-read/hybrid, exception rows) vs finance (Schedule entity materializing real rows, past immutable). **finSight already chose the finance model (AD-002), so adopt finance data-model + calendar edit-UX.** Consecrated edit UX = "this / this-and-following / all" (Google Calendar); "this and following" is a **series SPLIT** (cap old at UNTIL, new definition for the tail), not a bulk row update — maps perfectly to our materialized rows, past untouched. Research validates introducing a **`RecurrenceDefinition` entity** (retires legacy `frequency`/B-001) + a one-time migration of existing `seriesId` groups. We do NOT need lazy/hybrid expansion. **Architecture CONVERGED & LOCKED (2026-07-16, see research.md "Decisions LOCKED" D1–D6):** finance-materialized model + `RecurrenceDefinition` entity + calendar edit-UX (só este / este e os seguintes / todos); **series keeps ONE identity — NO physical split** (occurrences ARE the history since we materialize); "todos" spans everything incl. past, **no warning**; past immutable by default (prospective edits); endless recurrence = rolling-window generation w/ `generatedThrough` watermark (RECURRING only, not installments; deferred to post-v1 but entity enables it); one-time migration backfills a definition per `seriesId`. The `RecurrenceDefinition` entity is the single piece unlocking all three threads (edit + single identity + endless). **Still open for Specify/Discuss:** installment k/N relabel on "seguintes"; preserve vs clobber manual single-occurrence overrides; v1 scope (all three scopes vs seguintes+single-row first). User will proceed to Specify/Design in a later session — architecture groundwork is done.

---

## Todos

- [x] Reconcile README drift (Java 17, port 3000, no Flyway, correct env var names, docker-compose runs the API not a DB) — done 2026-07-05; added a root README with run instructions + SDD pointer
- [x] Decide whether to `git init` the monorepo root — done 2026-07-25: root repo created, both sub-repo histories merged in under their directories (233 commits), root files (`.specs/`, `.claude/`, `CLAUDE.md`) versioned for the first time. Sub-repo `.git` directories are gone; `lcsmrt/finSight-backend` and `-frontend` on GitHub are now stale
- [ ] Automate Portainer redeploy on push (GitHub Action → `PUT /stacks/{id}/git/redeploy`), mirroring mindmap's `deploy.yml` — see `DEPLOY.md`
- [ ] Low priority: backend dotenv filename is `.env.${spring.profiles.active:.local}` → resolves oddly (`.env..local`) with no active profile; confirm how `.env` is actually loaded locally

---

## Preferences

**Model Guidance Shown:** never
